# SwachhLens Backend — Production Dependencies
# FastAPI + SQLite/PostgreSQL + Auth

# Core
fastapi==0.141.1
uvicorn==0.52.1
python-dotenv==1.2.2
python-multipart==0.0.32

# Database
sqlalchemy==2.0.51
psycopg2-binary==2.9.12

# Auth & Security
email-validator==2.3.0

# AI / Vision (optional — falls back to demo if not installed)
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
Pillow>=10.0.0

# Utilities
httptools==0.8.0
watchfiles==1.2.0
